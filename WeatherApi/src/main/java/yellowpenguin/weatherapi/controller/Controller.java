package yellowpenguin.weatherapi.controller;

public class Controller {
	
	public void uniqueCall() {
		// Llamar al servicio
		// El servicio comprobara si existe y no ha caducado devolvéra la info.
		// Si no existe o ha caducado hará una llamada a la api de weather para obtener la info
		// Si ha recibido respuesta satisfactoria devolverá la info y la guardará/sobreescribirá en redis
		// Pero esto ya para mañana.
	
	}

}
